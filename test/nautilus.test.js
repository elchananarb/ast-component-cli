// const calculator = require("../bin/commands/commands_init");

// test("string with a single number should result in the number itself", () => {
//   expect(calculator.to_delete_check_test_cli(1)).toBe(1);
// });
